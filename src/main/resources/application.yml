spring:
    application:
        name: todos-cache
    cloud:
        stream:
            bindings:
                onCreatedEvent:
                    destination: todoCreatedEvent
                onDeletedEvent:
                    destination: todoDeletedEvent
                onUpdatedEvent:
                    destination: todoUpdatedEvent
                fireCreatedEvent:
                    destination: todoCreatedEvent
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
logging:
    level:
        io:
            corbs: DEBUG
management:
    endpoints:
        web:
            base-path: /ops
            exposure:
                include: '*'
server:
    port: 8002