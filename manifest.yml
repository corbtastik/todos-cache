---
applications:
- name: todos-cache
  memory: 1G
  routes:
  - route: todos-cache.cfapps.io
  path: target/todos-cache-1.0.0.SNAP.jar
  buildpack: java_buildpack
  services:
   - todos-cache
   - todos-messaging
  env:
    JAVA_OPTS: '-Deureka.client.serviceUrl.defaultZone=http://cloud-index.cfapps.io/eureka/'
